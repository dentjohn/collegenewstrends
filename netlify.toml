[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."analyze"]
  # Netlify Functions v2 supports streaming responses natively
  # The .mjs extension enables ES module format
